# AGENTS.md — 悬浮课表 · 项目工作指引

## 项目定位

个人自用的 Windows 桌面悬浮课程表（Electron 绿色单 exe）。**简单优先**：只加必要的功能，不引入未在 docs\02 中声明的依赖，界面全中文。

## 标准文档索引（以仓库内这些文件为准则）

| 文件 | 内容 | 何时读 |
|---|---|---|
| [docs\01-需求文档.md](docs/01-需求文档.md) | 功能需求与验收口径（需求以它为准） | 动任何功能前 |
| [docs\02-技术方案.md](docs/02-技术方案.md) | 选型、窗口实现、数据格式、打包、风险 | 涉及窗口/数据/IPC/打包时 |
| [docs\03-设计规范.md](docs/03-设计规范.md) | 颜色、字号、间距、交互状态的唯一标准 | **写 UI 前必读**，改样式先改它 |
| [docs\04-执行步骤.md](docs/04-执行步骤.md) | 五阶段计划与各阶段验收清单 | 每个阶段开始/验收时 |
| [开发日志\开发日志.md](开发日志/开发日志.md) | 已完成事项 / 待办事项的流水记录 | 每阶段结束时更新 |

## 工作说明

1. **按阶段推进**：严格执行 docs\04 的阶段顺序，一次只做一个阶段，不跨阶段堆功能。
2. **动手前**：功能口径查 01；窗口/数据查 02；UI 查 03。
3. **阶段收尾**：`npm start` 跑该阶段验收清单（打包相关项在阶段 5 统一验）→ 在开发日志追加一条「完成事项 / 待办事项」并更新待办看板勾选。
4. **改动规范**：需求、设计若需调整，先改对应文档再改代码，保持文档与实现一致。
5. **数据**：运行数据是用户自用的 schedule.json（exe 同目录），不要拿测试假数据覆盖；开发时可用默认值骨架。

## 常用命令

```bash
npm start        # 开发运行（electron .）
npm run dist     # 打包 Windows 便携版（产物在 dist\）
```

## 目录结构

```
src\
├─ main.js      # 主进程：窗口、数据读写、开机自启
├─ preload.js   # contextBridge 白名单 API
└─ renderer\    # index.html / style.css / app.js / settings.js（app.js 为课表主体）
```

## 代码规约

- 渲染层原生 JS，无框架无打包器；IPC 一律走 preload 白名单 API
- 顶栏拖拽区与可点击元素必须正确设置 `-webkit-app-region`（坑：忘设 no-drag 按钮点不动）
- 时间/日期处理注意本地时区；当前周计算规则见 docs\02 第 5 节
- 所有用户可见文案为中文
