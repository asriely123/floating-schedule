# 悬浮课表 — 执行步骤（分阶段计划）

> 原则：每阶段做完即运行验收、更新开发日志，再进下一阶段；不跨阶段堆功能。通用检查项：`npm start` 无报错、中文无乱码、无多余依赖。

## 阶段 0 · 规划与文档 —— ✅ 2026-09-04 完成

需求确认；创建 docs 四份标准文档、AGENTS.md、开发日志；制定本计划。

## 阶段 1 · 项目骨架与悬浮窗 —— ✅ 2026-09-04 完成

**目标**：窗口能出现、好看、能拖、能缩放、能记住位置。
**步骤**：`npm init` + 安装 electron（记录版本号到开发日志）→ 建 src 目录与 package.json 入口 → main.js 窗口代码（F1–F4：透明窗口 + CSS 半透明卡片、桌面挂件置底、无边框、skipTaskbar）→ 位置记忆与屏幕边界校验 → 顶栏占位（标题 + ⚙ 按钮）→ 空网格骨架 + 首次提示。
**验收清单**：
- [x] 右下角出现磨砂淡蓝半透明无边框窗
- [x] 桌面挂件模式：失焦后置底；拖动顺滑；可拖角缩放，缩不破最小尺寸
- [x] 重启后位置/大小还原；若手动把窗口拖出屏幕外重启，自动回右下角
- [x] ⚙ 按钮可点击（no-drag 生效）

## 阶段 2 · 课表网格与数据 —— ✅ 2026-09-04 完成

**目标**：课表本体——数据层 + 网格渲染 + 编辑 + 翻页。
**步骤**：schedule.json 读写（含默认值、.bak 备份、损坏容错）→ 渲染 5 天 × M 节网格与时间列 → 双击编辑课程名（Enter/失焦保存、Esc 取消）→ 周头（第 N 周/共 M 周、◀▶、回本周）→ 按开学日期自动定位当前周 → 复制上一周。
**验收结果（自动化 E2E·真实启动，`SCHEDULE_E2E=1` 钩子）**
- [x] 默认数据渲染出 5 × 8 网格，时间列显示占位作息
- [x] 点格子改课名，重启后仍在（跨两次启动交叉验证）
- [x] 翻页互不干扰（第 2 周编辑不影响第 3 周）
- [x] 「复制上一周」将上周内容带入本周（磁盘核对 weeks["3"] == weeks["2"]）
- [x] 开学日期（2026-09-07 周一）→ 9/4 定位第 1 周（开学前 clamp 正确）；未设置时提示条 + 回本周禁用
- 注：E2E 前先清空 schedule.json 的 weeks 再跑，避免历史数据污染断言；测试示例数据已清空

## 阶段 3 · 设置面板 —— ✅ 2026-09-04 完成

**目标**：所有自定义项可改、可持久化。
**步骤**：设置浮层 UI → 每天课数/节次时间联动网格 → 总周数/开学日期 → 不透明度 → 开机自启开关 → 退出程序按钮。
**验收结果（E2E3 断言 + 磁盘核对）**
- [x] 每天课数 8→9：草稿联动时间列表，保存后网格行数=9；旧周数据按行补齐（数组长度 9）且原内容保留
- [x] 逐条改起止时间：第 1 节改 08:10 → 时间列立即更新并落盘
- [x] 开学日期：保存后提示条消失、「回本周」启用，当前周计算按新日期
- [x] 不透明度滑块：input 事件即时预览（CSS 变量），保存落盘（0.8），重启回填
- [x] 开机自启：开关经 IPC（setLoginItemSettings）；dev 模式不验收（打包后阶段 5 验）
- [x] 「退出程序」按钮可退出（app:quit）；取消/✕/Esc 关闭且还原透明度预览
- [x] 重开设置：字段值从已保存数据回填（reopen-persisted）
- 补充：`scripts/reset-data.js`（--semester= 参数）用于 E2E 前置清数据；开机自启真实生效验收延后到阶段 5（dev 模式会把 electron.exe 写进自启）

## 阶段 4 · 高亮与打磨 —— ✅ 2026-09-04 完成

**目标**：F10 + 细节质感。
**步骤**：今天列高亮 + 当前节高亮（30 秒刷新）→ 当前周「· 本周」标记 → 格子过渡动画微调（hover/编辑态/空格子 + 在阶段 2/3 已就位并复核）。
**验收结果（E2E4 断言 + 截图）**
- [x] 今天列：整列淡蓝底色 + 表头深蓝加粗（实测周五列 9/9 格 + 表头正确）
- [x] 当前节：与 DOM 时间表独立计算的期望值一致；非课堂时段正确无高亮（下课自动恢复，30 秒轮询）
- [x] 周末：今天列为空（dayIndex>4 时不加任何高亮类）
- [x] 周标签「· 本周」标记（仅开学日期已设且视图=当前周时显示）
- [x] hover/编辑态/空格子 + 符合 docs\03；无边框窗口阴影/边距维持既有规范

## 阶段 5 · 打包与总验收 —— ✅ 2026-09-04 完成

**目标**：交付绿色单 exe。
**步骤**：安装 electron-builder（26.15.3）→ 打包配置（productName 中文 / `FloatingSchedule` exe 名 / icon.ico / portable 单文件）→ 打包 → 打包版全量 E2E → 便携壳子数据落位验证 → 清理测试数据。
**验收结果**
- [x] `dist\FloatingSchedule-0.1.0-portable.exe`（约 95MB）双击即用
- [x] 打包版（win-unpacked 同内容）完整 E2E 15 项全过（阶段 1–4 全部断言在打包产物上复跑）
- [x] 开机自启真实生效：`SCHEDULE_TEST_AUTOSTART` 钩子验证 enable→true / disable→false，路径指向打包 exe
- [x] 便携壳子实测：数据自动生成在 exe 旁（schedule.json + .bak），截图自检流程正常
- [x] 数据落位修正：打包版 dataDir 统一为 exe 所在目录（complement PORTABLE_EXECUTABLE_DIR）
- [x] 已清理 dist 内测试数据
- 交付提醒：打包版与开发模式的数据文件相互独立；开发模式录入的课表若需带进打包版，把项目根目录 schedule.json 拷到 exe 旁即可

## 维护修复 · 三阶段计划 —— 2026-09-04 启动

### 修复阶段 1 · 数据安全与保存可靠性（✅ 已验收）

统一 `schedule.json` 契约；主进程校验并归一化数据；主文件损坏时从 `.bak` 恢复；临时文件原子保存；程序目录不可写时回退到 `userData`；IPC 明确返回保存错误。

### 修复阶段 2 · 交互可靠性（✅ 已验收）

消除开机自启读取竞态；为保存失败提供用户可见提示；为“复制上一周”增加确认或可撤销保护；校验节次时间的合法性与不重叠性；支持格子编辑时 `Shift+Enter` 换行、`Enter` 保存。

### 修复阶段 3 · 可用性与收尾验收（进行中）

补齐键盘、焦点和浮层语义；限定当前节高亮只在当前查看周生效；收敛顶栏与功能文档；运行回归、打包与真机验收。
