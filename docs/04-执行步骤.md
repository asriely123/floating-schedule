# 悬浮课表 — 执行步骤（分阶段计划）

> 原则：每阶段做完即运行验收、更新开发日志，再进下一阶段；不跨阶段堆功能。通用检查项：`npm start` 无报错、中文无乱码、无多余依赖。

## 阶段 0 · 规划与文档 —— ✅ 2026-09-04 完成

需求确认；创建 docs 四份标准文档、AGENTS.md、开发日志；制定本计划。

## 阶段 1 · 项目骨架与悬浮窗 —— ✅ 2026-09-04 完成

**目标**：窗口能出现、好看、能拖、能缩放、能记住位置。
**步骤**：`npm init` + 安装 electron（记录版本号到开发日志）→ 建 src 目录与 package.json 入口 → main.js 窗口代码（F1–F4：透明窗口 + CSS 半透明卡片、桌面挂件置底、无边框、skipTaskbar）→ 位置记忆与屏幕边界校验 → 顶栏占位（标题 + ⚙ 按钮）→ 空网格骨架 + 首次提示。
**验收清单**：
- [x] 右下角出现磨砂淡蓝半透明无边框窗
- [x] 桌面挂件模式：失焦后置底；拖动顺滑；可拖角缩放，缩不破最小尺寸
- [x] 重启后位置/大小还原；若手动把窗口拖出屏幕外重启，自动回右下角
- [x] ⚙ 按钮可点击（no-drag 生效）

## 阶段 2 · 课表网格与数据 —— ✅ 2026-09-04 完成

**目标**：课表本体——数据层 + 网格渲染 + 编辑 + 翻页。
**步骤**：schedule.json 读写（含默认值、.bak 备份、损坏容错）→ 渲染 5 天 × M 节网格与时间列 → 双击编辑课程名（Enter/失焦保存、Esc 取消）→ 周头（第 N 周/共 M 周、◀▶、回本周）→ 按开学日期自动定位当前周 → 复制上一周。
**验收结果（自动化 E2E·真实启动，`SCHEDULE_E2E=1` 钩子）**
- [x] 默认数据渲染出 5 × 8 网格，时间列显示占位作息
- [x] 点格子改课名，重启后仍在（跨两次启动交叉验证）
- [x] 翻页互不干扰（第 2 周编辑不影响第 3 周）
- [x] 「复制上一周」将上周内容带入本周（磁盘核对 weeks["3"] == weeks["2"]）
- [x] 开学日期（2026-09-07 周一）→ 9/4 定位第 1 周（开学前 clamp 正确）；未设置时提示条 + 回本周禁用
- 注：E2E 前先清空 schedule.json 的 weeks 再跑，避免历史数据污染断言；测试示例数据已清空

## 阶段 3 · 设置面板 —— ✅ 2026-09-04 完成

**目标**：所有自定义项可改、可持久化。
**步骤**：设置浮层 UI → 每天课数/节次时间联动网格 → 总周数/开学日期 → 不透明度 → 开机自启开关 → 退出程序按钮。
**验收结果（E2E3 断言 + 磁盘核对）**
- [x] 每天课数 8→9：草稿联动时间列表，保存后网格行数=9；旧周数据按行补齐（数组长度 9）且原内容保留
- [x] 逐条改起止时间：第 1 节改 08:10 → 时间列立即更新并落盘
- [x] 开学日期：保存后提示条消失、「回本周」启用，当前周计算按新日期
- [x] 不透明度滑块：input 事件即时预览（CSS 变量），保存落盘（0.8），重启回填
- [x] 开机自启：开关经 IPC（setLoginItemSettings）；dev 模式不验收（打包后阶段 5 验）
- [x] 「退出程序」按钮可退出（app:quit）；取消/✕/Esc 关闭且还原透明度预览
- [x] 重开设置：字段值从已保存数据回填（reopen-persisted）
- 补充：`scripts/reset-data.js`（--semester= 参数）用于 E2E 前置清数据；开机自启真实生效验收延后到阶段 5（dev 模式会把 electron.exe 写进自启）

## 阶段 4 · 高亮与打磨 —— ✅ 2026-09-04 完成

**目标**：F10 + 细节质感。
**步骤**：今天列高亮 + 当前节高亮（30 秒刷新）→ 当前周「· 本周」标记 → 格子过渡动画微调（hover/编辑态/空格子 + 在阶段 2/3 已就位并复核）。
**验收结果（E2E4 断言 + 截图）**
- [x] 今天列：整列淡蓝底色 + 表头深蓝加粗（实测周五列 9/9 格 + 表头正确）
- [x] 当前节：与 DOM 时间表独立计算的期望值一致；非课堂时段正确无高亮（下课自动恢复，30 秒轮询）
- [x] 周末：今天列为空（dayIndex>4 时不加任何高亮类）
- [x] 周标签「· 本周」标记（仅开学日期已设且视图=当前周时显示）
- [x] hover/编辑态/空格子 + 符合 docs\03；无边框窗口阴影/边距维持既有规范

## 阶段 5 · 打包与总验收 —— ✅ 2026-09-04 完成

**目标**：交付绿色单 exe。
**步骤**：安装 electron-builder（26.15.3）→ 打包配置（productName 中文 / `FloatingSchedule` exe 名 / icon.ico / portable 单文件）→ 打包 → 打包版全量 E2E → 便携壳子数据落位验证 → 清理测试数据。
**验收结果**
- [x] `dist\FloatingSchedule-0.1.0-portable.exe`（约 95MB）双击即用
- [x] 打包版（win-unpacked 同内容）完整 E2E 15 项全过（阶段 1–4 全部断言在打包产物上复跑）
- [x] 开机自启真实生效：`SCHEDULE_TEST_AUTOSTART` 钩子验证 enable→true / disable→false，路径指向打包 exe
- [x] 便携壳子实测：数据自动生成在 exe 旁（schedule.json + .bak），截图自检流程正常
- [x] 数据落位修正：打包版 dataDir 统一为 exe 所在目录（complement PORTABLE_EXECUTABLE_DIR）
- [x] 已清理 dist 内测试数据
- 交付提醒：打包版与开发模式的数据文件相互独立；开发模式录入的课表若需带进打包版，把项目根目录 schedule.json 拷到 exe 旁即可

## 维护修复 · 三阶段计划 —— 2026-09-04 已批准

### 修复阶段 1 · 启动路径与数据安全 —— ✅ 2026-09-04 用户验收完成

**目标**：修正便携版路径、数据权威性、窗口字段所有权和备份恢复边界，保证自检不改用户状态。
**步骤**：统一 `schedule.json` 契约与主进程归一化 → 便携版用 `PORTABLE_EXECUTABLE_DIR` 定位数据目录、用 `PORTABLE_EXECUTABLE_FILE` 定位外层 exe 自启目标 → 替换历史临时解压路径自启项 → 以现有 exe 旁 `schedule.json` 为权威数据 → 由主进程独占 `window` 字段并合并保存 → 仅备份有效主文件，损坏主文件不得覆盖有效 `.bak` → 保留原自启状态的自检 → 临时文件原子保存、不可写时回退 `userData`、IPC 返回保存错误。
**验收清单（可执行）**：

- [x] 在打包 portable 运行环境检查：`PORTABLE_EXECUTABLE_DIR` 等于外层 exe 所在目录，`PORTABLE_EXECUTABLE_FILE` 等于外层 exe 完整路径；数据文件写在 exe 旁。
- [x] 预置 exe 旁 `schedule.json` 与回退目录不同内容后启动，读取结果以 exe 旁主文件为准；目录不可写时才使用 `userData`，并记录实际数据目录。
- [x] 启用开机自启后检查登录项路径指向 `PORTABLE_EXECUTABLE_FILE`；确认历史指向 portable 临时解压目录的旧项已被替换，不再保留临时路径。
- [x] 移动/缩放窗口后保存课表或设置，检查 `schedule.json.window` 等于主进程实际窗口边界；渲染层提交的数据不能覆盖该字段。
- [x] 隔离存储自检覆盖语法损坏、空对象和缺关键字段的主文件：均从有效 bak 恢复，修复保存后 bak 字节不变；无有效 bak 时回退默认值且不崩溃。
- [x] 运行自启自检钩子前分别准备原本开启、原本关闭两种状态，检查自检结束后状态分别保持不变。
- [x] 运行 `npm start` 回归：无报错、保存失败有 IPC 错误返回，原子保存后主文件可正常读取。

**已完成的自动化验证**：`npm start` 搭配隔离存储钩子通过；渲染层伪造 `window` 字段不能覆盖主进程值；portable 暂存打包成功；正式 exe 自检记录的目标为 `dist\FloatingSchedule-0.1.0-portable.exe`，探测前后自启状态恢复；替换正式 exe 前后 `dist\schedule.json` 与 `schedule.bak` 哈希不变。其余项目保留为真实用户数据、窗口行为和 Windows 重启验收，不以测试数据代替。

### 修复阶段 2 · 保存可靠性与界面闭环 —— ✅ 2026-09-04 用户验收完成

**目标**：界面显示完成时数据已经持久化；失败就近可见、可重试或回滚。`data` 是已提交的权威数据。
**步骤**：为课程编辑、翻周和复制上周建立“先持久化、后提交界面”的状态边界 → 设置面板内区分自启读取、节次校验、设置保存错误 → 保存按钮显示「保存中…」且只锁设置面板 → 无开学日期时不显示今天/当前节高亮并提供「设置开学日期」入口 → 收敛 4/14 节边界、移除未声明的 30 字静默限制 → 补齐可点击控件状态。
**验收清单**：

- [x] 人为制造写入失败时，格子编辑、翻周、复制上周和设置均保持当前 `data`/输入，不先提交界面；在受影响对象附近显示中文错误，重试成功后才落盘并更新界面。
- [x] 设置错误只出现在设置面板内，并分别显示自启读取、节次校验、设置保存原因；设置失败保留草稿和位置，取消设置不改变原数据。
- [x] 设置保存按钮显示「保存中…」且只锁设置面板；主课表仍可见，保存失败后可再次重试。
- [x] 未设置开学日期时不显示今天/当前节高亮，并可从顶部提示的「设置开学日期」入口直接打开设置。
- [x] 4 节时隐藏删除、14 节时隐藏添加；没有可点击但无结果的控件；不再静默限制课程名为 30 字；交互控件具备 pointer、hover、active、disabled 状态，禁用态为 not-allowed。

**已完成的自动化验证**：开发版与独立暂存 portable 均通过隔离 E2E；覆盖超 30 字多行课程完整保存、翻周与复制、无开学日期不高亮及提示入口、4/14 节边界、节次校验保留草稿、「保存中…」状态和设置持久化。portable 自启探测完成 `false → true → false` 恢复，正式 exe 替换前后个人 `schedule.json` 与 `schedule.bak` 哈希不变。写入失败的四条人工验收仍保留给用户，不以阶段 3 的失败注入测试门禁提前代替。

**真机反馈修正（已被第二次反馈否定）**：首次方案在 Windows 原生确认框关闭后由主进程重新激活窗口，但真机仍出现“DOM 编辑器已显示、Windows 键盘焦点未恢复”。阶段 3 收尾改为应用内模态确认层，从源头避免确认动作触发桌面挂件 `blur → HWND_BOTTOM`；自动化必须用 `webContents.sendInputEvent()` 验证实际键盘事件，不能再通过脚本直接赋值制造假阳性。

**用户验收结果**：用户已确认全部验收项通过，修复阶段 2 完成。

### 修复阶段 3 · 测试门禁、布局与最终交付 —— ✅ 2026-09-04 用户验收完成

**目标**：建立不污染个人课表、失败可阻断交付的验收流程，并完成真机收尾。
**步骤**：正式 `npm test` 自动创建系统临时数据目录、设置超时并以非零退出码报告失败 → 所有 `SCHEDULE_*` 钩子均要求 `SCHEDULE_TEST_MODE=1`，测试模式的自启使用内存实现 → 一次性保存失败注入覆盖格子编辑、翻周、复制上一周和设置 → 解决 460×320 最小窗口的两行顶栏、窄屏网格宽度与设置滚动 → 增加 CSP、阻断新窗口/非入口导航、校验 IPC sender 与入口 URL、限制自启参数为 bool → 监听运行时显示器事件，窗口不可见且边界失效时恢复主屏 → 完成 Explorer 重启、真实多屏、托盘、置底和真实开机自启人工验收 → 收敛 README、标准文档、开发日志与最终 exe。

**实现契约（本阶段未完成前不得宣称通过）**：

- `npm test` 是正式隔离回归入口。它必须使用系统临时目录作为数据目录，设置明确超时并清理临时目录；不得触碰项目根目录、便携版目录或用户现有数据。任一断言失败、进程异常退出或超时均返回非零退出码。
- 所有 `SCHEDULE_*` 测试钩子（截图、E2E、存储、自启及派生钩子）只有 `SCHEDULE_TEST_MODE=1` 时才启用。测试模式的开机自启为内存自启，不写 Windows 登录项；真实自启探测是单独受保护流程，记录原状态，完成或失败后都恢复，无法精确恢复则跳过写入式探测。
- 通过一次性保存失败注入分别验证课程格子编辑、翻周（与「回本周」共用同一 `goWeek` 提交边界）、「复制上一周」和设置保存：失败时保持已提交 `data`、输入、查看周、网格和设置草稿不变，受影响控件附近显示中文错误，恢复后可重试。
- 在 460×320 视口，顶栏自动两行且内容不裁切；窄屏网格时间列 72px、间距 4px、每天列至少 64px。设置浮层只有 body 滚动，header/footer 固定可见。
- 入口页面设置 CSP；阻断新窗口和非入口导航；每个 IPC 请求校验 sender 及入口 URL；`setAutoStart` 仅接受布尔值。
- 监听 `display-added`、`display-removed`、`display-metrics-changed`；窗口边界不与任一显示器工作区相交（几何位置不可见）时恢复到主屏工作区右下角并持久化，边界仍可见时不擅自移动。
- Explorer 重启、真实多显示器切换、托盘显示/隐藏、失焦置底和真实便携版开机自启仍是人工验收项，自动化钩子结果不能替代用户真机通过。
**验收清单**：

- [x] `npm test` 一条命令可安全完成全量回归：临时目录隔离、超时生效、失败返回非零，且不污染个人数据。
- [x] 所有 `SCHEDULE_*` 钩子在无 `SCHEDULE_TEST_MODE=1` 时不启用；测试模式自启不写系统，真实自启探测保留原状态。
- [x] 写入失败注入覆盖格子编辑、翻周、复制上一周和设置，四条路径均保持未提交状态并可见错误、可重试。
- [x] 键盘、焦点循环、设置 body 单一滚动、header/footer 固定、保存锁定、非本周高亮及 460×320 代表性最小视口通过自动验收和截图复核。
- [x] CSP、新窗口/非入口导航阻断、IPC sender+入口 URL 校验、自启 bool 校验及运行时显示器事件恢复主屏通过验证。
- [x] `npm run dist` 后完成人工 Explorer 重启、真实多屏、托盘、置底、真实开机自启、便携版数据落位和多行课程验收。
- [x] README、标准文档、开发日志、代码和最终便携 exe 状态一致。

**自动验收结果**：`npm test` 全部通过并自动清理临时目录；最终 portable 在独立临时数据目录复跑存储、阶段 2–4、失败重试、最小视口和安全断言，退出码为 0。460×320（本机 DPI 取整为 461×321）截图确认顶栏、网格和设置固定区无裁切。真实自启探测完成 `false → true → false` 恢复；正式 exe 替换前后个人 `schedule.json` 与 `schedule.bak` SHA-256 均保持 `1D6E85B394FE98028583281AABFE41CBC66FBC1C0C034A6490DF16753D0348EE`。

**用户验收结果**：用户已确认 Explorer、显示器、托盘、置底、自启、便携版数据落位和手工输入等全部验收项通过，修复阶段 3 完成。

**复制后编辑二次修复结果**：用户真机确认首次焦点补丁无效后，已删除原生 `window.confirm` 并改为应用内确认层。重新执行的开发版与最终 portable 回归均确认：打开、Esc 取消、再次确认期间 BrowserWindow `blur` 为 `0 → 0`；确认复制后 `win.isFocused()`、`document.hasFocus()` 和编辑器 `activeElement` 同时成立；`webContents.sendInputEvent()` 可输入字符并按 Enter 保存；用户随后已通过真实鼠标与输入法环境复验。

## 显示周末新功能 · 三阶段计划 —— 2026-09-05 已批准

### 新功能阶段 1 · 需求契约与数据兼容 —— ✅ 2026-09-05 完成

**目标**：在不改变现有五列界面的前提下，让数据层安全支持周一至周日，并为旧课表补齐默认关闭的设置字段。

**步骤**：更新需求、技术、设计与执行文档 → 增加 `settings.showWeekend: false` → 主进程固定归一化星期索引 0..6 → 旧文件缺字段时保持五列默认 → 更新重置数据骨架 → 增加隔离存储自检。

**验收清单**：

- [x] 旧数据缺少 `showWeekend` 时归一化为 `false`，现有课程、设置、窗口与查看周不变。
- [x] 数据层接受并保留星期索引 5/6；关闭显示或再次保存时不得删除周末课程。
- [x] 周末数组按 `periodsPerDay` 归一化，非字符串课程转换为空串；不存在周末数据时不要求预先写入空数组。
- [x] `scripts/reset-data.js` 的新数据默认 `showWeekend: false`。
- [x] 存储自检在系统临时目录通过，且本阶段不修改渲染结构、不显示周末列、不触碰个人课表。

**验收结果**：`npm test` 全量通过，正式门禁包含 `weekend-data-compatibility`；默认五列回归仍为 40 个格子。个人 `dist\schedule.json` 与 `dist\schedule.bak` 在测试前后哈希不变。阶段 2 的设置控件、动态列渲染与样式尚未开始。

### 新功能阶段 2 · 设置、渲染与完整交互 —— ✅ 2026-09-05 完成

**目标**：加入「显示周末」设置，并让七列模式完整支持编辑、翻周、复制和高亮。

**步骤**：设置面板增加默认关闭的复选框 → 由设置草稿统一提交 `showWeekend` → 表头、格子和高亮共享 5/7 天可见范围 → 周末格沿用既有编辑保存 → 复制上一周包含周末 → 关闭后隐藏但保留数据 → 增加隔离 E2E。

**验收清单**：

- [x] 旧数据与首次启动默认显示周一至周五；设置面板正确回填未勾选状态。
- [x] 勾选不会立即改变课表，保存成功后才在周五右侧增加周六、周日；重开设置仍为勾选。
- [x] 设置保存失败时主课表保持原列数，复选框草稿与错误信息保留，重试成功后才切换。
- [x] 周六、周日格支持双击/键盘进入编辑、Enter/失焦保存、Shift+Enter 换行和 Esc 取消。
- [x] 翻周保持每周独立；复制上一周同时复制周末课程。
- [x] 关闭并保存后恢复五列，再次开启时周末课程完整恢复。
- [x] 开启周末且查看本周时，周六、周日与工作日使用相同的今天列和当前节高亮规则。
- [x] `npm test` 全量通过；本阶段不打包，不触碰个人课表。

**验收结果**：默认状态为 5 列、8 节、40 格；开启并保存后表头与课程行均为 7 天（9 节测试态共 63 格、8 条网格轨道）。隔离 E2E 已覆盖草稿不预览、持久化回填、周六多行编辑、键盘取消、失焦保存、复制周末、隐藏保留、重新显示、七天高亮和写入失败重试。复制保存完成后重新恢复「复制上周」按钮 DOM 焦点，消除保存期间按钮禁用导致的焦点丢失。阶段 3 的双布局截图、完整响应式门禁、README 与最终 portable 尚未开始。

### 新功能阶段 3 · 响应式、测试门禁与交付 —— ✅ 2026-09-05 用户验收完成

**目标**：完成五/七列布局回归、最小视口测试、README/日志收口及最终便携版交付。

**步骤**：补齐 460×320 最小窗口下五列与七列的独立几何断言 → 验证七列表头、课程行与横向滚动保持同步且页面本身不横向溢出 → 固化五列/七列代表性截图 → 让同一隔离回归脚本可直接验收源码和最终 portable → 更新 README 与开发日志 → 备份个人课表后构建、隔离复跑并原样恢复个人数据。

**验收清单**：

- [x] 默认五列在 460×320 下保持每天列至少 64px，顶栏、时间列与网格不裁切。
- [x] 七列在 460×320 下由课表滚动容器独占横向滚动；表头与课程行宽度、列轨道和滚动位置一致，页面本身不横向溢出。
- [x] 五列与七列代表性截图通过视觉复核；设置浮层仍只有 body 纵向滚动，header/footer 固定可见。
- [x] `npm test` 全量通过，且新增五列/七列最小视口断言和截图文件均为正式门禁。
- [x] 最终 `dist\FloatingSchedule-0.1.0-portable.exe` 使用系统临时数据目录复跑同一套隔离回归并通过。
- [x] 构建前后个人 `dist\schedule.json` 与 `dist\schedule.bak` 哈希一致；README、标准文档和开发日志与最终产物一致。

**自动验收结果**：Windows 当前 DPI 下最小窗口实际为 461×321。五列宽 414px、每天列最小 64.39px且无需横向滚动；七列宽 548px、每天列 64px，由课表容器提供 134px 横向滚动，滚至最右端后表头与课程行的宽度、左边界及八条轨道完全一致。开发版 `npm test` 与最终 portable 隔离全量回归均以退出码 0 通过，五列、七列和设置面板截图已人工复核。

**交付结果**：最终 `dist\FloatingSchedule-0.1.0-portable.exe` SHA-256 为 `F059D808F8D129461EF4F0242D0E553AD7AA58E45C40A982D87B683588F6423A`。构建前后个人 `schedule.json` SHA-256 均为 `7B796F06FBF6B927CC6445B32B4A3CE963571806CC837937C2AB98DD77935393`，`schedule.bak` 均为 `500E89FB6BE61B16EF3FBC803C587761C2BDB693872648C3BAD8E55B271DD20D`；它们未进入 Git 变更。

**用户验收结果**：默认五列、周末切换、横向滚动、周末编辑/复制/高亮、隐藏后数据保留及 portable 重启持久化已全部验收通过，新功能三阶段完成。
