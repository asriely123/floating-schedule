# 悬浮课表 · 开发日志

> 说明：每个阶段性工作结束时，由 Claude 自动在下方追加一条：`日期 · 阶段 · 完成事项 / 待办事项`。待办看板同步更新勾选状态。

## 待办看板

- [x] 阶段 1 · 项目骨架与悬浮窗
- [x] 阶段 2 · 课表网格与数据
- [x] 阶段 3 · 设置面板
- [x] 阶段 4 · 高亮与打磨
- [x] 阶段 5 · 打包与总验收
- [x] 维护修复阶段 1 · 数据安全与保存可靠性
- [x] 维护修复阶段 2 · 交互可靠性
- [ ] 维护修复阶段 3 · 可用性与收尾验收（进行中）

---

## 2026-09-04 · 阶段 0 · 规划与文档

**完成事项**
- 与用户逐项确认需求（见 docs\01-需求文档.md）：Electron 便携版、每周独立编辑、周一至周五、置顶/拖动/缩放/记忆位置/开机自启/当前天与当前节高亮
- 新增规划性决策：开学日期设置项（自动定位当前周所需）、窗口不透明度滑块、退出程序入口、数据备份 schedule.bak
- 创建 docs\01~04 四份标准文档（需求/技术/设计/执行步骤）
- 创建 AGENTS.md 工作指引、本开发日志

**待办事项**
- 阶段 1：项目骨架与悬浮窗（验收清单见 docs\04-执行步骤.md）

---

## 2026-09-04 · 阶段 1 · 项目骨架与悬浮窗 ✅

**完成事项**
- 初始化 npm 项目，安装 Electron 44.1.1（官方源二进制下载失败，改用 npmmirror 镜像成功——后续加依赖同样处理）
- 建立 src 结构：main.js（窗口/存储/位置记忆/自检）/ preload.js / renderer（index.html、style.css、app.js）
- 窗口（F1–F4）：无边框 + Acrylic 磨砂 + 置顶 + 不占任务栏；默认 640×480 右下角偏移 24px；最小 460×320；可拖可缩放
- 位置记忆：schedule.json 的 `window` 字段；move/resize 防抖 400ms 保存 + 关闭兜底；启动校验可见性，越界自动回右下角
- 数据存储骨架：默认值重建容错、schedule.bak 备份；便携版数据目录用 `PORTABLE_EXECUTABLE_DIR`（已补记 docs\02）
- 渲染骨架：顶栏（标题 + 第 1 周/共 20 周 + ⚙ 按钮）+ 5 列 × 8 行网格 + 时间列，样式符合 docs\03
- 开发自检钩子：`SCHEDULE_SHOT=1` 截图 + 样式断言后自动退出；`SCHEDULE_TEST_RESIZE=1` 模拟改尺寸

**验证结果（自动化）**
- [x] 默认出现在右下角（实测坐标 1043,515）
- [x] 拖拽区正确：顶栏 `drag`、⚙ 按钮 `no-drag` 且点击回调触发
- [x] 窗口改尺寸 700×520 → 重启还原为 702×524（DPI 取整，正常）
- [x] 伪造越界坐标 (90000,90000) → 重启自动回右下角
- [x] 截图 dev-screenshot.png：淡蓝半透明卡片、网格/时间列排版正常；时间格确认为透明无边框
- [ ] 待用户真机确认：Acrylic 毛玻璃观感、拖动/缩放顺滑度

**待办事项**
- 阶段 2 · 课表网格与数据（IP 数据读写、点格编辑、按周翻页、复制上周、定位当前周）

---

## 2026-09-04 · 阶段 1 · 用户真机反馈修复轮 ✅

**用户反馈 3 条 → 处理**
1. 无托盘小图标（原 v1 不做清单里排除了，用户提出新增）→ 已加 **F12 托盘**：scripts/make-icon.js 纯 Node 生成 assets/tray.png（32px 淡蓝圆角 + 白课表行，另有 256px icon.png 备打包）；Tray 左键切换显示/隐藏，右键菜单「显示 / 隐藏课表」「退出」；需求文档同步（01 新增 F12、不做清单移除托盘）
2. 窗口不透明（根因排查）：
   - 实时观测 + 真实屏幕截图（desktopCapturer 技术，dev-screen-crop.png / dev-screen-full.png）确认：`backgroundMaterial:'acrylic'` 在 Electron 44.1.1 + 本机 Win11 26200（DPI 1.5）**完全不生效**，窗口呈纯色不透明；`getBackgroundMaterial()` 也读不到值（unknown）
   - **方案切换**：`transparent:true` + CSS 自绘卡片（margin 8px + 圆角 12 + 阴影）；底色 0.75 → 0.65
   - 像素级验证（R−B 色差剖面法）：卡片区 R−B ≈ −19，完全不透明应为 −34，实测与「0.65×底色 + 0.35×壁纸」的混合预测（−20.4）吻合 → **半透明确认生效**
   - docs\02 已更新为透明窗口方案并记录诊断结论
3. 行高不随窗口变化 → `#grid-body` 改 flex 列 + `.cell-row flex:1`（行最小高 44px），行高随窗口均分；顺带修复因此引入的滚动条 bug（#scroll 改 flex 列，grid-body min-height:100% 移除）；截图确认真实窗口高度下 8 行均分填满、无滚动条

**自动化验证**：拖拽区断言全过（header=drag / 按钮=no-drag / ⚙ 可点 / 8 行 40 格 / 时间格透明无边框）；真机截图确认置顶悬浮于其他窗口之上、阴影圆角正常
**待用户复验**：托盘图标是否出现、半透明观感、拖动/缩放（透明窗口下边角缩放）手感

---

## 2026-09-04 · 阶段 2 · 课表网格与数据 ✅

**完成事项**
- 数据打通：preload 暴露 `load()` / `save()`（store:load / store:save IPC）；渲染层持有全量数据，编辑即保存（.bak 备份沿用主进程逻辑）
- 网格按设置渲染：`settings.periodsPerDay` 决定行数，`periodTimes` 决定时间列（缺时间行留空）
- 点格内联编辑：单击出输入框并全选，Enter/失焦确认、Esc 取消，30 字上限；空格子 hover 显示 +
- 周导航：◀▶ 翻页（边界禁用）、回本周、周标签「第 N 周 ／ 共 M 周」，每周内容独立
- 复制上周：一键深拷贝到本周（第 1 周禁用）
- 当前周计算：按 `semesterStart`（第一周周一）计算并夹取 [1, weekCount]；未设置显示提示条「尚未设置开学日期」且回本周禁用
- 自动化 E2E 钩子：`SCHEDULE_E2E=1`（翻页/编辑/独立性/复制/回本周 5 项断言）

**验证结果**
- [x] 5 项 E2E 断言全过；两次运行交叉验证持久化（第二次从磁盘读到历史数据）
- [x] 磁盘结构核对：`weeks["2"]["0"]=["高等数学"]`、`weeks["3"]` 为复制结果
- [x] 截图确认新顶栏按钮渲染与禁用态正确
- [x] 用户调整（2026-09-04）：格编辑触发由「单击」改为「双击」（E2E 与 docs\03 同步）
- [x] 用户追加功能（2026-09-04）：记住上次查看的周——新增顶层字段 `viewWeek`，翻页即保存，启动校验（1..weekCount，否则回退当前周）后恢复；E2E 两次启动交叉验证（initial-restore-week2 PASS）；docs\01/02 已同步
- 待用户真机体验：点格编辑、翻页手感

**待办事项**
- 阶段 3 · 设置面板（每天课数、节次时间、总周数、开学日期、不透明度、开机自启、退出程序）
- 说明：测试数据已清理；开学日期置空，由用户在设置面板填真实日期

---

## 2026-09-04 · 阶段 3 · 设置面板 ✅

**完成事项**
- 设置浮层（整窗覆盖 + 遮罩，非新窗口）：⚙ 打开；✕/取消/Esc 关闭（还原透明度预览）；保存提交
- 字段：每天课数（4–14）、节次时间列表（每行开始/结束 time + ✕ 删除、「＋ 添加一节」，与课数联动）、总周数（1–30）、开学日期（date）、不透明度（滑块 0.5–1，input 即时预览，CSS 变量 --alpha）、开机自启（经 IPC → setLoginItemSettings）、退出程序（app:quit）
- 保存动作：settings 提交 + periodTimes 与课数对齐（补空/截断）→ 旧周数据按行补齐/截断且保留原内容 → viewWeek 越界收敛 → 存盘 → 重新渲染
- 辅助：scripts/reset-data.js（E2E 前置清数据，--semester= 可选，保留窗口位置）；E2E 新增 runPhase3E2E（6 项断言）
- 自检增强：SHOT 流程截图后验证设置浮层可开 + 单独截取设置面板图（dev-screenshot-settings.png）

**验证结果**
- [x] E2E3 六项全过：open-settings / draft-rows-9（草稿即时联动）/ grid-rows-9 / time-updated（08:10 落网格）/ hint-hidden / alpha-applied（rgba ..0.8）/ reopen-persisted（重启回填）
- [x] 磁盘核对 8 项全过：periodsPerDay=9、periodTimes[0]=08:10、长度 9、semesterStart、opacity=0.8、weeks["2"]["0"] 长度 9（补齐且保留「高等数学」）、viewWeek=2
- [x] 截图确认设置面板视觉符合 design spec（dev-screenshot-settings.png）
- 待办：开机自启真实生效需打包后验证（阶段 5，dev 模式会写入 electron.exe 的自启项故不在 dev 验收）

---

## 2026-09-04 · 阶段 4 · 高亮与打磨 ✅

**完成事项**
- F10 高亮：`refreshHighlight()`（30 秒 setInterval 轮询 + 每次渲染后调用）——今天列整列淡蓝底 + 表头深蓝加粗；当前节课实底 #4A90D9 白字；周末/非课时段自动无高亮
- 周标签「· 本周」标记（开学日期已设且视图为当前周时显示）
- 格子过渡动画（background/color 0.12s）；`window.__refreshHighlight` 供 E2E 消除时钟差
- E2E4：页面内独立计算期望（真实时钟 + DOM 时间表）对比实际高亮类，两断言（今天列/当前节，含周末分支）

**验证结果**
- [x] 15 项 E2E 全过（阶段 2 回归 5 + 阶段 3 六项 + 阶段 4 两项）
- [x] 实测今天=周五：周五列 9/9 格 + 表头高亮正确；00:30 非课时段 current-period 正确为空
- [x] 截图确认：周五列淡蓝整列、周标签「· 本周」
- 注：期间清理了用户验收时残留的 electron.exe 进程（TaskStop 只杀了 npm，子进程需 taskkill //F //IM electron.exe）

**待办事项**
- 阶段 5 · 打包与总验收（electron-builder 便携版 + 开机自启真机验证 + 全部验收清单复跑）

---

## 2026-09-04 · 阶段 5 · 打包与总验收 ✅

**完成事项**
- electron-builder 26.15.3（npm 源可用，打包工具链走 npmmirror binaries 镜像）
- 打包配置：appId com.floating.schedule；productName 悬浮课表；executableName/artifactName 用 ASCII（FloatingSchedule-0.1.0-portable.exe）；win.icon = assets/icon.ico（make-icon.js 新增 ICO 容器输出，内嵌 256px PNG，零依赖）
- 产物：dist\FloatingSchedule-0.1.0-portable.exe（约 95MB）+ win-unpacked 同内容目录
- 主进程增强：打包版 dataDir 统一为 exe 所在目录（portable 用环境变量、其余用 exe 路径，避免落进 asar）；`SCHEDULE_SHOT_DIR` 输出目录环境变量（打包版 asar 不可写）；`SCHEDULE_TEST_AUTOSTART` 自检钩子（注册→读回→撤销）
- 工具链备忘：`ELECTRON_BUILDER_BINARIES_MIRROR` + `ELECTRON_MIRROR` 两个镜像变量在打包时必须带

**验证结果**
- [x] 打包版（win-unpacked，与便携内容一致）完整 E2E **15 项全过**（含阶段 1–4 全部断言在打包产物复跑）
- [x] 开机自启真实生效：enable->true / disable->false（路径=打包 exe）
- [x] 便携壳子实测：启动自动在 exe 旁创建 schedule.json + .bak；SHOT 自检流程在便携实例内正常跑完并自动退出
- [x] dist 内测试数据已清理；无残留进程
- 待用户最终真机验收：双击 exe 体验完整功能

---

## 2026-09-04 · 阶段 5 修订 · 桌面挂件模式 + 托盘救援菜单 ✅

**用户改动需求**
1. 取消「始终置顶」，改为**桌面挂件模式**（只能在桌面之上、其他一切窗口之下）
2. 托盘右键菜单新增「恢复默认位置」（窗口拖丢时救援）

**实现**
- F3 模式切换：`alwaysOnTop:false` + koffi 调 Win32 `SetWindowPos(HWND_BOTTOM)`（首个运行时依赖 koffi 3.2.0，预编译无构建；已确认可在 Electron 44 主进程与 asar 包内正常加载）；置底时机 = show 后 150ms + blur；点击窗口可临时前置编辑，失焦回落
- F12 救援：`restoreDefaultPosition()` 复用 defaultBounds 回右下角并写回 schedule.json（启动越界校验依旧兜底）
- 排查记录：静默退出现象根因是**先前便携测试实例未退出**（NSIS 壳子 + 应用进程均名为 FloatingSchedule.exe，早前只过滤了 electron.exe）——单实例锁被占导致新实例无输出直接退出；已全部清理（`taskkill //F //IM FloatingSchedule.exe`）

**验证结果**
- [x] 开发版完整 E2E（14/14）回归通过；`[Z] window pinned to bottom` 置底日志确认执行
- [x] 重新打包（含 koffi）后 win-unpacked 抽查：`[Z]` 置底生效、位置记忆正常
- [x] 产物：dist\FloatingSchedule-0.1.0-portable.exe（已更新）；dist 测试数据已清理、无残留进程
- 待用户真机验收：窗口是否确实沉到其他窗口之下（最小化其他窗口/点桌面即可见）、托盘「恢复默认位置」

---

## 2026-09-04 · 项目收尾清理 ✅

- 删除开发期快照 4 张（dev-screenshot / dev-screenshot-settings / dev-screen-full / dev-screen-crop）与一次性诊断脚本 scripts/analyze-crop.js（Acrylic 排查用，话题已关闭）
- 自检截图默认输出目录改为系统临时目录（`os.tmpdir()`，可用 SCHEDULE_SHOT_DIR 覆盖），避免再污染项目根；源码变更后已重新打包（dist exe 与源码一致）
- dist 清理：win-unpacked（369MB，可再生）、builder-debug.yml、测试数据 schedule.* 删除；最终交付物 = `dist\FloatingSchedule-0.1.0-portable.exe`
- 项目最终结构：AGENTS.md / src / assets / docs / 开发日志 / scripts（make-icon、reset-data、test-koffi）/ package.json / schedule.json(+bak) / dist（单 exe）
- 新增 README.md：项目/应用说明（功能、快速开始、打包、数据、目录结构、技术栈、Vibe Coding 开发方式、已知说明）

---

**环境记录**：Node v24.18.0 / npm 11.16.0 / Electron 44.1.1 / electron-builder 26.15.3 / koffi 3.2.0（npmmirror 镜像安装）/ Windows 11 26200（Acrylic 实测失效，透明窗口方案，DPI 1.5）。Electron 二进制下载失败时用 `ELECTRON_MIRROR=https://npmmirror.com/mirrors/electron/`；打包时加 `ELECTRON_BUILDER_BINARIES_MIRROR=https://npmmirror.com/mirrors/electron-builder-binaries/`。单实例锁被占时检查并清理：`taskkill //F //IM electron.exe` 与 `taskkill //F //IM FloatingSchedule.exe`。

---

## 2026-09-04 · 维护修复阶段 1 · 数据安全与保存可靠性 ✅

**完成事项**

- 技术方案与执行步骤同步：明确 `weeks` 使用 `0..4` 对应周一至周五；修正默认不透明度、窗口模式、预加载 API、目录结构和开机自启自检变量；新增三阶段维护修复计划。
- 主进程新增 `schedule.json` 归一化：限制每天课数（4–14）、总周数（1–30）、不透明度（0.5–1）、窗口尺寸、周/日/节次数据结构和课程名长度；无效日期、时间和字段类型降级为安全默认值。
- 存储读取新增 `.bak` 恢复；保存改为临时文件写入、旧文件备份、原子替换主文件；程序目录不可写时回退至 Electron `userData`。
- `store:save` 不再吞掉写入异常，IPC 调用方可收到失败；窗口位置等无界面调用仅记录错误，不使应用崩溃。
- 新增 `SCHEDULE_STORE_E2E=1` 隔离自检：仅在系统临时目录验证数据归一化与备份恢复，不读写真实 `schedule.json`。
- `node --check src/main.js`、`src/preload.js`、`src/renderer/app.js` 已通过。

**验收状态**

- [x] 用户验收通过：隔离存储自检与数据安全行为已确认。

**待办事项**

- 维护修复阶段 2：处理设置、自启、复制、时间校验及格子多行编辑交互。

---

## 2026-09-04 · 维护修复阶段 2 · 交互可靠性（实现完成，待验收）

**完成事项**

- 设置面板打开后为本次会话建立标识；开机自启状态读取完成前禁用自启开关和保存按钮，关闭面板后的迟到响应不再修改已清理草稿。
- 设置保存改为异步提交边界：仅锁定设置面板；开机自启与数据均写入成功后才关闭面板。失败时保留所有已输入内容并在课表顶部显示错误；若数据保存失败，尝试还原本次已变更的系统自启状态。
- 新增节次时间校验：开始/结束必须同时填写、结束晚于开始，且列表顺序不得重叠。
- 「复制上一周」在覆盖本周课程前显示一次确认，不确认则不改动数据。
- 格子编辑器由单行输入框改为多行文本框：`Enter` 保存，`Shift+Enter` 插入换行；课程单元格保留换行显示与保存。
- 阶段 2 E2E 已适配多行课程、复制确认和自启状态读取等待；`node --check src/main.js`、`src/preload.js`、`src/renderer/app.js` 已通过。

**待验收事项**

- [ ] 双击格子后，输入内容并按 `Shift+Enter` 能换行，按 `Enter` 能保存，重开应用后换行仍在。
- [ ] 设置面板打开时自启开关短暂不可提交；关闭后不报错；错误时间段不允许保存且提示原因。
- [ ] 点击「复制上一周」先出现覆盖确认；取消后本周课程保持不变。
- [ ] 人为使课表文件不可写时，保存失败提示出现，设置面板和已输入内容不消失。

**验收结果**

- [x] 用户验收通过，进入维护修复阶段 3。

---

## 2026-09-04 · 维护修复阶段 3 · 可用性与收尾验收（实现完成，待验收）

**完成事项**

- 课表格补齐键盘入口与语义：格子可聚焦，Enter/空格进入编辑；编辑态移除按钮语义，结束后恢复；课程内容作为可读标签提供，并保留多行展示。
- 设置浮层改为具有 `dialog` / `aria-modal` 语义的标题、内容、操作区三段结构；内容区为唯一纵向滚动区，Tab 在浮层内循环，关闭后焦点回到 ⚙。
- 设置保存期间禁用所有设置输入、保存、取消、关闭、退出与 Esc，防止请求进行时丢弃或混入修改；读取自启状态期间仍可关闭。
- 仅在当前查看周等于本周时显示当天与当前节高亮，浏览历史或未来周不再产生误导性“当前”状态。
- 统一顶栏信息层级与设计规范，并同步需求、README、技术说明。README 已说明：开发模式会登记 `electron.exe`，但登录后不能可靠启动课表；开机自启只在打包版验收。
- `node --check src/main.js`、`src/preload.js`、`src/renderer/app.js` 已通过；`npm run dist` 已成功生成 `dist\FloatingSchedule-0.1.0-portable.exe`（100,289,699 字节）。

**待验收事项**

- [ ] Tab 聚焦格子，Enter/空格进入编辑；设置浮层内 Tab 不会离开浮层，关闭后焦点回到 ⚙。
- [ ] 在历史/未来周不显示今天列与当前节；回到本周后恢复高亮。
- [ ] 设置内容较长时只有内容区滚动，标题与底部操作始终可见；保存过程中无法关闭或修改面板。
- [ ] 运行新生成的便携版，确认基本课表、托盘、设置与多行课程均正常。

**说明**

- 未运行内置 `SCHEDULE_E2E`：该测试会写入课程测试数据，不能用于包含真实个人课表的当前工作区。其余静态检查与打包已完成。
